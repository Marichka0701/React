const baseURL = 'https://jsonplaceholder.typicode.com';

const endPoints = {
    todos: '/todos',
    albums: '/albums',
    comments: '/comments',
    posts: '/posts',
}

export {
    baseURL,
    endPoints,
}