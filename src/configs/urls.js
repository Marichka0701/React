const baseURL = 'https://jsonplaceholder.typicode.com/';

const endPoints = {
    posts: '/posts',
    comments: '/comments',
    albums: '/albums',
    photos: '/photos',
    todos: '/todos',
    users: '/users',
}

export {
    baseURL,
    endPoints,
}